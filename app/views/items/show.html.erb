<div class="container">
  <div class="row">
    <div class="col-md-8 col-md-offset-2">
      <div class="page-header">
        <h1><%= @item.title %></h1>
      </div>

      <h3>Description:</h3>
      <p><%= @item.description %></p>

      <h3>Type:</h3>
      <p><%= @item.toy_type.title %></p>

      <%= link_to 'Edit', edit_item_path(@item), class: "btn btn-default" %>
      <%= link_to 'Back', items_path, class: "btn btn-default" %>
    </div>
  </div>
</div>
